# Production Environment Variables
# Replace these values with your actual production values when deploying

NODE_ENV=production
PORT=10000

# Use a MongoDB Atlas connection string for production
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/suvvidha?retryWrites=true&w=majority

# Use a strong, random string for JWT_SECRET in production
JWT_SECRET=replace_with_a_strong_random_string